# React 完全学習ロードマップ (Pure React & SPA Architecture)

## 第1章：React開発環境とJSXの基本
* **開発環境 (Vite)**
    * ViteによるReactプロジェクトの作成
    * プロジェクト構造の理解 (main.jsx, App.jsx)
    * Strict Modeについて
* **JSXのルールと記法**
    * JSXの仕組みとJavaScript式の埋め込み
    * フラグメント (<>...</>) と単一要素
    * 属性の指定 (className, styleオブジェクト)
    * 条件付きレンダリング (&&, 三項演算子)
    * リストレンダリング (map) と key 属性

## 第2章：コンポーネント開発の基礎 (Props & State)
* **Props (プロパティ)**
    * 親から子へのデータ受け渡し
    * Propsの分割代入
    * children プロップスの活用
* **State (状態)**
    * useState フックの基本
    * 状態の更新と再レンダリングの仕組み
    * オブジェクトや配列のState更新 (スプレッド構文, Immer)
* **イベントハンドリング**
    * イベントオブジェクトの扱い
    * フォーム入力の基本 (onChange)
    * 子から親へのデータ伝達 (コールバック関数)

## 第3章：副作用とフック (Hooks) の基本
* **副作用の制御 (useEffect)**
    * useEffectの基本構文
    * 依存配列 (dependency array) の正しい設定
    * クリーンアップ関数 (アンマウント時の処理)
    * **注意点**: 無限ループと不要な実行を防ぐ
* **DOM参照と値の保持 (useRef)**
    * DOM要素へのアクセス
    * 再レンダリングを起こさない変数の保持

## 第4章：フォーム実装とバリデーション
* **React Hook Form**
    * フォーム管理の基本 (register, handleSubmit)
    * バリデーションルールの実装
    * エラーメッセージの表示
    * UIライブラリとの連携
* **スキーマバリデーション**
    * Zod または Yup を用いたバリデーション定義

## 第5章：スタイリングとコンポーネント設計
* **コンポーネント設計**
    * Atomic Design の概念とディレクトリ構成
    * プレゼンテーションコンポーネントとコンテナコンポーネント
* **スタイリング手法**
    * CSS Modules
    * CSS-in-JS (Emotion / styled-components)
    * Tailwind CSS (概要とセットアップ)
* **UIカタログ (Storybook)**
    * Storybookの導入
    * ストーリーの作成と管理

## 第6章：パフォーマンス最適化とメモ化
* **再レンダリングの仕組み**
    * レンダリングが起きる条件
    * React.memo によるコンポーネントのメモ化
* **フックによるメモ化**
    * useCallback (関数のメモ化)
    * useMemo (計算結果のメモ化)
* **レンダリングの計測**
    * React Developer Tools (Profiler) の使い方

## 第7章：ルーティング (React Router)
* **React Router v6+**
    * ルーティングの設定 (BrowserRouter, Routes, Route)
    * ページ遷移 (Link, useNavigate)
    * URLパラメータ (useParams) とクエリパラメータ
    * ネストされたルート (Outlet)
    * 404ページの実装

## 第8章：グローバル状態管理
* **Context API**
    * createContext と Provider
    * useContext の利用
    * パフォーマンスの注意点
* **Recoil / Jotai** (または Zustand)
    * Atom の概念
    * グローバルステートの更新と参照
    * どのライブラリを選ぶべきかの指針

## 第9章：API連携とデータフェッチ
* **データ取得の基本**
    * useEffect + fetch/axios による取得
    * ローディングとエラー状態の管理
* **データフェッチライブラリ (SWR / React Query)**
    * キャッシュ機能と自動再検証
    * カスタムフック化によるロジック分離

## 第10章：React × TypeScript 実践
* **コンポーネントの型定義**
    * Propsの型定義 (type / interface)
    * イベントハンドラーの型
    * children (React.ReactNode) の型
* **Hooksの型定義**
    * useState<T>, useRef<T>
    * カスタムフックの型
* **型安全な開発**
    * 型定義ファイル (d.ts) の扱い
    * 共通の型定義管理

## 第11章：テスト (Vitest & React Testing Library)
* **単体テスト**
    * テスト環境の構築 (Vitest)
    * コンポーネントのレンダリングテスト
    * ユーザー操作のシミュレーション
* **モックと非同期テスト**
    * APIモック (MSW)
    * 非同期更新のテスト

## 第12章：実務レベルのSPAアーキテクチャ構築
* **堅牢なプロジェクトセットアップ**
    * Vite + TypeScript + UIライブラリ (Chakra UI/MUI) の統合設定
    * スケーラブルなディレクトリ構成のベストプラクティス
* **認証機能のアーキテクチャ**
    * ログイン状態の永続化と復元
    * PrivateRoute (保護されたルート) の実装
    * 認証コンテキストの設計
* **APIクライアントの共通化**
    * Axiosインスタンスの設定 (Interceptors)
    * リクエスト/レスポンスの共通処理
* **UIフィードバックの共通化**
    * グローバルなエラーハンドリング機構
    * 画面全体を覆うローディング (Overlay Loading) の管理
* **CRUDロジックの抽象化**
    * 再利用可能なデータアクセス層の設計