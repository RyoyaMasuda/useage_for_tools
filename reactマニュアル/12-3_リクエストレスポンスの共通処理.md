# 12-3. リクエスト/レスポンスの共通処理

この章では、Axiosインスタンスを使用したリクエスト/レスポンスの共通処理について学びます。共通処理を実装することで、エラーハンドリングや認証トークンの付与などを一元管理できます。

---

## 目次

- [リクエスト/レスポンスの共通処理とは](#リクエストレスポンスの共通処理とは)
- [リクエストインターセプター](#リクエストインターセプター)
- [レスポンスインターセプター](#レスポンスインターセプター)
- [エラーハンドリング](#エラーハンドリング)
- [実践例: 完全なAPIクライアント](#実践例-完全なapiクライアント)
- [まとめ](#まとめ)
- [演習問題](#演習問題)

---

## リクエスト/レスポンスの共通処理とは

### 共通処理の必要性
APIリクエストでは、以下のような処理を毎回実装する必要があります：
- **認証トークンの付与**: リクエストヘッダーにトークンを追加
- **エラーハンドリング**: エラーレスポンスの統一的な処理
- **ローディング状態**: リクエスト中のローディング表示
- **リトライ処理**: 失敗時の自動リトライ

### 共通処理の利点
- **コードの重複削減**: 同じ処理を何度も書く必要がない
- **一貫性**: すべてのAPIリクエストで同じ処理が適用される
- **保守性**: 変更が必要な場合、1箇所を修正するだけで済む
- **エラー処理の統一**: エラーハンドリングを一元管理できる

### Axiosインターセプター
Axiosインターセプターを使用することで、リクエストとレスポンスの前後に処理を追加できます。

---

## リクエストインターセプター

### リクエストインターセプターとは
リクエストインターセプターは、**リクエストが送信される前に実行される処理**です。

### 基本的な使い方

```typescript
import axios from 'axios'

const apiClient = axios.create({
  baseURL: 'https://api.example.com',
})

// リクエストインターセプター
apiClient.interceptors.request.use(
  (config) => {
    // リクエスト送信前の処理
    return config
  },
  (error) => {
    // エラー処理
    return Promise.reject(error)
  }
)
```

### 実践例: 認証トークンの付与

```typescript
// src/api/client.ts
import axios, { AxiosRequestConfig } from 'axios'

const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || 'https://api.example.com',
  timeout: 10000,
})

// リクエストインターセプター: 認証トークンを付与
apiClient.interceptors.request.use(
  (config) => {
    // ローカルストレージからトークンを取得
    const token = localStorage.getItem('authToken')
    
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

export default apiClient
```

### 実践例: リクエストログの出力

```typescript
// src/api/client.ts
import axios from 'axios'

const apiClient = axios.create({
  baseURL: 'https://api.example.com',
})

// リクエストインターセプター: ログ出力
apiClient.interceptors.request.use(
  (config) => {
    console.log(`[Request] ${config.method?.toUpperCase()} ${config.url}`)
    console.log('Request Data:', config.data)
    console.log('Request Headers:', config.headers)
    
    return config
  },
  (error) => {
    console.error('[Request Error]', error)
    return Promise.reject(error)
  }
)

export default apiClient
```

### 実践例: リクエストIDの付与

```typescript
// src/api/client.ts
import axios from 'axios'

const apiClient = axios.create({
  baseURL: 'https://api.example.com',
})

// リクエストインターセプター: リクエストIDを付与
apiClient.interceptors.request.use(
  (config) => {
    // リクエストIDを生成
    const requestId = `req_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`
    config.headers['X-Request-ID'] = requestId
    
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

export default apiClient
```

---

## レスポンスインターセプター

### レスポンスインターセプターとは
レスポンスインターセプターは、**レスポンスが返ってきた後に実行される処理**です。

### 基本的な使い方

```typescript
import axios from 'axios'

const apiClient = axios.create({
  baseURL: 'https://api.example.com',
})

// レスポンスインターセプター
apiClient.interceptors.response.use(
  (response) => {
    // レスポンス成功時の処理
    return response
  },
  (error) => {
    // エラー処理
    return Promise.reject(error)
  }
)
```

### 実践例: レスポンスデータの変換

```typescript
// src/api/client.ts
import axios, { AxiosResponse } from 'axios'

const apiClient = axios.create({
  baseURL: 'https://api.example.com',
})

// レスポンスインターセプター: データの変換
apiClient.interceptors.response.use(
  (response: AxiosResponse) => {
    // APIレスポンスが { data: {...}, status: 200 } の形式の場合
    if (response.data && response.data.data) {
      return response.data.data
    }
    return response.data
  },
  (error) => {
    return Promise.reject(error)
  }
)

export default apiClient
```

### 実践例: レスポンスログの出力

```typescript
// src/api/client.ts
import axios, { AxiosResponse } from 'axios'

const apiClient = axios.create({
  baseURL: 'https://api.example.com',
})

// レスポンスインターセプター: ログ出力
apiClient.interceptors.response.use(
  (response: AxiosResponse) => {
    console.log(`[Response] ${response.config.method?.toUpperCase()} ${response.config.url}`)
    console.log('Response Status:', response.status)
    console.log('Response Data:', response.data)
    
    return response
  },
  (error) => {
    if (error.response) {
      console.error(`[Response Error] ${error.config.method?.toUpperCase()} ${error.config.url}`)
      console.error('Error Status:', error.response.status)
      console.error('Error Data:', error.response.data)
    } else {
      console.error('[Response Error]', error.message)
    }
    return Promise.reject(error)
  }
)

export default apiClient
```

---

## エラーハンドリング

### エラーハンドリングの重要性
APIリクエストでは、様々なエラーが発生する可能性があります：
- **401 Unauthorized**: 認証エラー
- **403 Forbidden**: 権限エラー
- **404 Not Found**: リソースが見つからない
- **500 Internal Server Error**: サーバーエラー

### 実践例: 統一的なエラーハンドリング

```typescript
// src/api/client.ts
import axios, { AxiosError } from 'axios'

const apiClient = axios.create({
  baseURL: 'https://api.example.com',
})

// レスポンスインターセプター: エラーハンドリング
apiClient.interceptors.response.use(
  (response) => {
    return response
  },
  (error: AxiosError) => {
    if (error.response) {
      const { status, data } = error.response
      
      switch (status) {
        case 401:
          // 認証エラー: ログインページにリダイレクト
          localStorage.removeItem('authToken')
          window.location.href = '/login'
          break
          
        case 403:
          // 権限エラー: エラーメッセージを表示
          console.error('アクセス権限がありません')
          break
          
        case 404:
          // リソースが見つからない
          console.error('リソースが見つかりません')
          break
          
        case 500:
          // サーバーエラー
          console.error('サーバーエラーが発生しました')
          break
          
        default:
          console.error('予期しないエラーが発生しました')
      }
      
      // エラーメッセージを返す
      return Promise.reject({
        status,
        message: (data as any)?.message || 'エラーが発生しました',
        data
      })
    } else if (error.request) {
      // リクエストは送信されたが、レスポンスが返ってこない
      console.error('ネットワークエラーが発生しました')
      return Promise.reject({
        status: 0,
        message: 'ネットワークエラーが発生しました',
      })
    } else {
      // リクエストの設定中にエラーが発生
      console.error('リクエストの設定中にエラーが発生しました')
      return Promise.reject({
        status: 0,
        message: error.message,
      })
    }
  }
)

export default apiClient
```

### 実践例: エラーメッセージの統一

```typescript
// src/api/client.ts
import axios, { AxiosError } from 'axios'

interface ApiError {
  status: number
  message: string
  data?: any
}

const apiClient = axios.create({
  baseURL: 'https://api.example.com',
})

// エラーメッセージのマッピング
const errorMessages: Record<number, string> = {
  400: 'リクエストが不正です',
  401: '認証が必要です',
  403: 'アクセス権限がありません',
  404: 'リソースが見つかりません',
  500: 'サーバーエラーが発生しました',
  503: 'サービスが利用できません',
}

// レスポンスインターセプター: エラーメッセージの統一
apiClient.interceptors.response.use(
  (response) => {
    return response
  },
  (error: AxiosError): Promise<ApiError> => {
    if (error.response) {
      const { status, data } = error.response
      const message = 
        (data as any)?.message || 
        errorMessages[status] || 
        'エラーが発生しました'
      
      return Promise.reject({
        status,
        message,
        data
      } as ApiError)
    }
    
    return Promise.reject({
      status: 0,
      message: 'ネットワークエラーが発生しました',
    } as ApiError)
  }
)

export default apiClient
```

---

## 実践例: 完全なAPIクライアント

### 実践例1: 認証付きAPIクライアント

```typescript
// src/api/client.ts
import axios, { AxiosError, AxiosRequestConfig, AxiosResponse } from 'axios'

interface ApiError {
  status: number
  message: string
  data?: any
}

const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || 'https://api.example.com',
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json',
  },
})

// リクエストインターセプター: 認証トークンの付与
apiClient.interceptors.request.use(
  (config: AxiosRequestConfig) => {
    const token = localStorage.getItem('authToken')
    
    if (token && config.headers) {
      config.headers.Authorization = `Bearer ${token}`
    }
    
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

// レスポンスインターセプター: エラーハンドリング
apiClient.interceptors.response.use(
  (response: AxiosResponse) => {
    return response.data
  },
  (error: AxiosError): Promise<ApiError> => {
    if (error.response) {
      const { status, data } = error.response
      
      // 401エラーの場合、ログインページにリダイレクト
      if (status === 401) {
        localStorage.removeItem('authToken')
        window.location.href = '/login'
      }
      
      const message = (data as any)?.message || 'エラーが発生しました'
      
      return Promise.reject({
        status,
        message,
        data
      } as ApiError)
    }
    
    return Promise.reject({
      status: 0,
      message: 'ネットワークエラーが発生しました',
    } as ApiError)
  }
)

export default apiClient
```

### 実践例2: リトライ機能付きAPIクライアント

```typescript
// src/api/client.ts
import axios, { AxiosError, AxiosRequestConfig, AxiosResponse } from 'axios'

const apiClient = axios.create({
  baseURL: 'https://api.example.com',
})

// リトライ設定
const MAX_RETRIES = 3
const RETRY_DELAY = 1000

// リトライ可能なエラーかどうかを判定
const isRetryableError = (error: AxiosError): boolean => {
  if (!error.response) {
    return true // ネットワークエラーはリトライ可能
  }
  
  const status = error.response.status
  // 500番台と408（タイムアウト）はリトライ可能
  return status >= 500 || status === 408
}

// リトライ処理
const retryRequest = async (
  config: AxiosRequestConfig,
  retryCount: number = 0
): Promise<AxiosResponse> => {
  try {
    return await axios(config)
  } catch (error) {
    const axiosError = error as AxiosError
    
    if (isRetryableError(axiosError) && retryCount < MAX_RETRIES) {
      // リトライ前に待機
      await new Promise(resolve => setTimeout(resolve, RETRY_DELAY * (retryCount + 1)))
      return retryRequest(config, retryCount + 1)
    }
    
    throw error
  }
}

// レスポンスインターセプター: リトライ処理
apiClient.interceptors.response.use(
  (response: AxiosResponse) => {
    return response
  },
  async (error: AxiosError) => {
    if (isRetryableError(error) && error.config) {
      try {
        return await retryRequest(error.config)
      } catch (retryError) {
        return Promise.reject(retryError)
      }
    }
    
    return Promise.reject(error)
  }
)

export default apiClient
```

### 実践例3: ローディング状態管理付きAPIクライアント

```typescript
// src/api/client.ts
import axios, { AxiosError, AxiosRequestConfig, AxiosResponse } from 'axios'

// ローディング状態を管理するStore（Zustandの例）
import { useLoadingStore } from '../stores/loadingStore'

const apiClient = axios.create({
  baseURL: 'https://api.example.com',
})

// リクエストインターセプター: ローディング開始
apiClient.interceptors.request.use(
  (config: AxiosRequestConfig) => {
    useLoadingStore.getState().setLoading(true)
    return config
  },
  (error) => {
    useLoadingStore.getState().setLoading(false)
    return Promise.reject(error)
  }
)

// レスポンスインターセプター: ローディング終了
apiClient.interceptors.response.use(
  (response: AxiosResponse) => {
    useLoadingStore.getState().setLoading(false)
    return response
  },
  (error: AxiosError) => {
    useLoadingStore.getState().setLoading(false)
    return Promise.reject(error)
  }
)

export default apiClient
```

### 実践例4: リクエスト/レスポンスのログ出力

```typescript
// src/api/client.ts
import axios, { AxiosError, AxiosRequestConfig, AxiosResponse } from 'axios'

const apiClient = axios.create({
  baseURL: 'https://api.example.com',
})

// 開発環境でのみログを出力
const isDevelopment = import.meta.env.DEV

// リクエストインターセプター: ログ出力
apiClient.interceptors.request.use(
  (config: AxiosRequestConfig) => {
    if (isDevelopment) {
      console.group(`🚀 [API Request] ${config.method?.toUpperCase()} ${config.url}`)
      console.log('Headers:', config.headers)
      console.log('Data:', config.data)
      console.log('Params:', config.params)
      console.groupEnd()
    }
    return config
  },
  (error) => {
    if (isDevelopment) {
      console.error('❌ [API Request Error]', error)
    }
    return Promise.reject(error)
  }
)

// レスポンスインターセプター: ログ出力
apiClient.interceptors.response.use(
  (response: AxiosResponse) => {
    if (isDevelopment) {
      console.group(`✅ [API Response] ${response.config.method?.toUpperCase()} ${response.config.url}`)
      console.log('Status:', response.status)
      console.log('Data:', response.data)
      console.groupEnd()
    }
    return response
  },
  (error: AxiosError) => {
    if (isDevelopment) {
      console.group(`❌ [API Response Error] ${error.config?.method?.toUpperCase()} ${error.config?.url}`)
      if (error.response) {
        console.log('Status:', error.response.status)
        console.log('Data:', error.response.data)
      } else {
        console.log('Error:', error.message)
      }
      console.groupEnd()
    }
    return Promise.reject(error)
  }
)

export default apiClient
```

---

## まとめ

この章では、Axiosインスタンスを使用したリクエスト/レスポンスの共通処理について学びました。

### 学んだこと
- リクエストインターセプター: リクエスト送信前の処理
- レスポンスインターセプター: レスポンス受信後の処理
- エラーハンドリング: 統一的なエラー処理
- 実践例: 完全なAPIクライアントの実装

### 重要なポイント
1. **リクエストインターセプター**: 認証トークンの付与、ログ出力など
2. **レスポンスインターセプター**: データ変換、エラーハンドリングなど
3. **エラーハンドリング**: 統一的なエラー処理で一貫性を保つ
4. **共通処理**: コードの重複を削減し、保守性を向上

### 次のステップ
次の章では、UIフィードバックの共通化について詳しく学びます。

---

## 演習問題

### 問題1: 認証トークンの付与
リクエストインターセプターを使用して、認証トークンをリクエストヘッダーに付与してください。

<details>
<summary>解答例</summary>

```typescript
// src/api/client.ts
import axios, { AxiosRequestConfig } from 'axios'

const apiClient = axios.create({
  baseURL: 'https://api.example.com',
})

apiClient.interceptors.request.use(
  (config: AxiosRequestConfig) => {
    const token = localStorage.getItem('authToken')
    
    if (token && config.headers) {
      config.headers.Authorization = `Bearer ${token}`
    }
    
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

export default apiClient
```
</details>

### 問題2: エラーハンドリング
レスポンスインターセプターを使用して、401エラーの場合にログインページにリダイレクトする処理を実装してください。

<details>
<summary>解答例</summary>

```typescript
// src/api/client.ts
import axios, { AxiosError } from 'axios'

const apiClient = axios.create({
  baseURL: 'https://api.example.com',
})

apiClient.interceptors.response.use(
  (response) => {
    return response
  },
  (error: AxiosError) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('authToken')
      window.location.href = '/login'
    }
    return Promise.reject(error)
  }
)

export default apiClient
```
</details>

### 問題3: レスポンスデータの変換
レスポンスインターセプターを使用して、APIレスポンスの`data`プロパティを直接返すように変換してください。

<details>
<summary>解答例</summary>

```typescript
// src/api/client.ts
import axios, { AxiosResponse } from 'axios'

const apiClient = axios.create({
  baseURL: 'https://api.example.com',
})

apiClient.interceptors.response.use(
  (response: AxiosResponse) => {
    return response.data
  },
  (error) => {
    return Promise.reject(error)
  }
)

export default apiClient
```
</details>

### 問題4: リクエストログの出力
リクエストインターセプターを使用して、リクエストのメソッド、URL、データをログ出力してください。

<details>
<summary>解答例</summary>

```typescript
// src/api/client.ts
import axios, { AxiosRequestConfig } from 'axios'

const apiClient = axios.create({
  baseURL: 'https://api.example.com',
})

apiClient.interceptors.request.use(
  (config: AxiosRequestConfig) => {
    console.log(`[Request] ${config.method?.toUpperCase()} ${config.url}`)
    console.log('Data:', config.data)
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

export default apiClient
```
</details>

### 問題5: 統一的なエラーメッセージ
レスポンスインターセプターを使用して、エラーレスポンスから統一的なエラーメッセージを返すようにしてください。

<details>
<summary>解答例</summary>

```typescript
// src/api/client.ts
import axios, { AxiosError } from 'axios'

interface ApiError {
  status: number
  message: string
}

const apiClient = axios.create({
  baseURL: 'https://api.example.com',
})

apiClient.interceptors.response.use(
  (response) => {
    return response
  },
  (error: AxiosError): Promise<ApiError> => {
    if (error.response) {
      const { status, data } = error.response
      const message = (data as any)?.message || 'エラーが発生しました'
      
      return Promise.reject({
        status,
        message
      } as ApiError)
    }
    
    return Promise.reject({
      status: 0,
      message: 'ネットワークエラーが発生しました'
    } as ApiError)
  }
)

export default apiClient
```
</details>

---

お疲れ様でした！次の章に進みましょう。

